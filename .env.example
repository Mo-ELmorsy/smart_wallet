# Example environment variables
# SmartWallet currently saves the Gemini API Key inside SharedPreferences via the Settings Screen.
# You do NOT need to set this in an .env file to run the app.
GEMINI_API_KEY=your_api_key_here
